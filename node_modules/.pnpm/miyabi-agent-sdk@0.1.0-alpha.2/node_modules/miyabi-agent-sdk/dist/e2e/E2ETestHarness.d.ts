/**
 * E2E Test Harness
 *
 * Miyabi自律型開発環境のエンドツーエンドテストフレームワーク
 *
 * Phase 8-3: Real API Integration Support
 */
export interface E2EScenario {
    id: number;
    name: string;
    description: string;
    issueTitle: string;
    issueBody: string;
    expectedComplexity: "small" | "medium" | "large" | "xlarge";
    expectedPriority: "P0" | "P1" | "P2" | "P3";
    expectedType: "bug" | "feature" | "refactor" | "docs" | "test" | "chore";
    successCriteria: {
        minQualityScore?: number;
        minCoverage?: number;
        maxDurationMs?: number;
        requiresSecurityScan?: boolean;
        budgetThresholds?: {
            warning: number;
            reject: number;
            emergency: number;
        };
    };
}
export interface E2ETestResult {
    scenarioId: number;
    scenarioName: string;
    success: boolean;
    duration: number;
    qualityScore?: number;
    coverage?: number;
    prNumber?: number;
    prUrl?: string;
    errors: string[];
    warnings: string[];
    metrics: {
        tasksCreated: number;
        parallelExecutions: number;
        budgetUsed: number;
    };
}
export interface E2ETestConfig {
    useRealAPI?: boolean;
    anthropicApiKey?: string;
    githubToken?: string;
    testRepo?: string;
    testOwner?: string;
}
/**
 * E2E Test Harness
 *
 * Agent間の統合フローをテストする
 *
 * Phase 8-3: Supports both mock and real API modes
 */
export declare class E2ETestHarness {
    private coordinatorAgent;
    private issueAgent;
    private codeGenAgent;
    private reviewAgent;
    private prAgent;
    private testAgent;
    private config;
    constructor(config?: E2ETestConfig);
    /**
     * シナリオ実行
     */
    runScenario(scenario: E2EScenario): Promise<E2ETestResult>;
    /**
     * 失敗結果を作成
     */
    private createFailedResult;
    /**
     * 全シナリオ実行
     */
    runAllScenarios(scenarios: E2EScenario[]): Promise<E2ETestResult[]>;
    /**
     * 結果表示
     */
    private printResult;
    /**
     * 総合レポート表示
     */
    private printSummary;
}
//# sourceMappingURL=E2ETestHarness.d.ts.map